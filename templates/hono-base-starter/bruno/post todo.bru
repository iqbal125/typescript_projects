meta {
  name: post todo
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/api/todos
  body: json
  auth: inherit
}

body:json {
  {
    "id": 2,
    "title": "test",
    "description": "Test"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
